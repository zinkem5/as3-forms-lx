{{!
  Deletes an entire tenant.
}}
{
  "class": "AS3",
  "action": "deploy",
  "persist": true,
  "declaration": {
    "class": "ADC",
    "schemaVersion": "3.0.0",
    "id": "00-{{tenant_name}}",
    "label": "DELETE {{tenant_name}}",
    "remark": "DELETE {{tenant_name}}",
    "{{tenant_name}}": {
      "class": "Tenant"
    }
  }
}
